<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.nasr.droidviewpoc">

    <!-- إعلان رؤية الخدمة المستهدفة -->
    <queries>
        <!-- للتواصل مع تطبيق DroidView -->
        <package android:name="com.eightksec.droidview" />
        <intent>
            <action android:name="com.eightksec.droidview.ITokenService" />
        </intent>
        <intent>
            <action android:name="com.eightksec.droidview.TOKEN_SERVICE" />
        </intent>
        <!-- لرؤية التطبيقات القابلة للتشغيل (Launcher) -->
        <intent>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent>
    </queries>

    <!-- الصلاحيات المطلوبة -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- إذا أردت الحصول على الرقم التسلسلي، أضف هذا الإذن (اختياري)
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    -->

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="DroidViewPOC"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        android:theme="@style/Theme.AppCompat.Light.DarkActionBar">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>